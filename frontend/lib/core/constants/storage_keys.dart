/// Keys used with [FlutterSecureStorage].
///
/// Centralised here so typos are caught at compile-time.
class StorageKeys {
  StorageKeys._();

  static const String jwtToken = 'jwt_token';
  static const String userProfile = 'user_profile';
}
